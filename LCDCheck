#include <Wire.h>
#include <rgb_lcd.h>  
rgb_lcd lcd; 

void setup() {
  lcd.begin(16, 2);  
  
  // Set RGB backlight color (values range from 0 to 255)
  lcd.setRGB(255, 0, 0); 
  lcd.setCursor(0, 0); 
  lcd.print("MISSILE INBOUND");  
}

void loop() {
}
